### Sprint 5 Kahoot Quiz

---

### Question 1 (Quiz)

**Question:** What is the purpose of `event.preventDefault()` in a form submission event listener?
**Time Limit:** 20 seconds
**Answers:**
1.  To submit the form.
2.  To validate the form fields.
3.  **To prevent the default form submission behavior (page reload). (Correct)**
4.  To clear the form fields.

---

### Question 2 (True/False)

**Question:** The `innerHTML` property is a safer way to set the content of an element than `textContent` because it automatically escapes HTML tags.
**Time Limit:** 20 seconds
**Answers:**
1.  True
2.  **False (Correct)**

---

### Question 3 (Quiz)

**Question:** What does the `target` property of the event object refer to?
**Time Limit:** 20 seconds
**Answers:**
1.  The event listener function.
2.  The parent element of the event target.
3.  **The element that triggered the event. (Correct)**
4.  The type of event that occurred.

---

### Question 4 (Quiz)

**Question:** Which `console` method is best for logging critical errors that include a stack trace?
**Time Limit:** 20 seconds
**Answers:**
1.  `console.log()`
2.  `console.warn()`
3.  **`console.error()` (Correct)**
4.  `console.info()`

---

### Question 5 (True/False)

**Question:** A `ReferenceError` occurs when you try to perform an operation on a value of the wrong type.
**Time Limit:** 20 seconds
**Answers:**
1.  True
2.  **False (Correct)**

---

### Question 6 (Quiz)

**Question:** What does the `debugger` statement do in JavaScript?
**Time Limit:** 20 seconds
**Answers:**
1.  It logs a message to the console.
2.  It throws an error.
3.  **It pauses the execution of the code and opens the debugger. (Correct)**
4.  It fixes bugs in the code automatically.

---

### Question 7 (Quiz)

**Question:** Which string method returns `true` if a string contains a specified value, and `false` otherwise?
**Time Limit:** 20 seconds
**Answers:**
1.  `indexOf()`
2.  `search()`
3.  **`includes()` (Correct)**
4.  `match()`

---

### Question 8 (True/False)

**Question:** The `switch` statement is a good alternative to a long `if...else if...else` statement.
**Time Limit:** 20 seconds
**Answers:**
1.  **True (Correct)**
2.  False

---

### Question 9 (Quiz)

**Question:** Which array method creates a new array with the results of calling a provided function on every element in the calling array?
**Time Limit:** 20 seconds
**Answers:**
1.  `forEach()`
2.  **`map()` (Correct)**
3.  `filter()`
4.  `reduce()`

---

### Question 10 (Quiz)

**Question:** Which array method checks if at least one element in the array passes the test implemented by the provided function?
**Time Limit:** 20 seconds
**Answers:**
1.  `every()`
2.  `find()`
3.  **`some()` (Correct)**
4.  `filter()`

---

### Question 11 (True/False)

**Question:** The `sort()` method, by default, sorts numbers in ascending numerical order.
**Time Limit:** 20 seconds
**Answers:**
1.  True
2.  **False (Correct)**

---

### Question 12 (Quiz)

**Question:** What is the purpose of the `git commit --amend` command?
**Time Limit:** 20 seconds
**Answers:**
1.  To create a new commit.
2.  To undo the last commit.
3.  **To modify the last commit. (Correct)**
4.  To view the commit history.
