### Sprint 2 Kahoot Quiz

---

### Question 1 (Quiz)

**Question:** What is the terminal command to change the current directory?
**Time Limit:** 20 seconds
**Answers:**
1.  `ls`
2.  `pwd`
3.  **`cd` (Correct)**
4.  `mkdir`

---

### Question 2 (True/False)

**Question:** `git` is a cloud service for hosting repositories, while `GitHub` is a command-line tool for version control.
**Time Limit:** 20 seconds
**Answers:**
1.  True
2.  **False (Correct)**

---

### Question 3 (Quiz)

**Question:** Which command is used to stage all changes in the current directory for a Git commit?
**Time Limit:** 20 seconds
**Answers:**
1.  `git commit -a`
2.  **`git add .` (Correct)**
3.  `git stage all`
4.  `git add *`

---

### Question 4 (Quiz)

**Question:** What is the purpose of the `<iframe>` element in HTML?
**Time Limit:** 20 seconds
**Answers:**
1.  To play audio files.
2.  To display images.
3.  **To embed external content, like a webpage or video, within the current page. (Correct)**
4.  To create a form.

---

### Question 5 (True/False)

**Question:** The `autoplay` attribute on a `<video>` element will always work, even if the video is not muted.
**Time Limit:** 20 seconds
**Answers:**
1.  True
2.  **False (Correct)**

---

### Question 6 (Quiz)

**Question:** Which CSS `transform` function is used to resize an element?
**Time Limit:** 20 seconds
**Answers:**
1.  `rotate()`
2.  `translate()`
3.  **`scale()` (Correct)**
4.  `skew()`

---

### Question 7 (Quiz)

**Question:** What is the purpose of the `transition` property in CSS?
**Time Limit:** 20 seconds
**Answers:**
1.  To create complex, multi-step animations.
2.  **To animate property changes smoothly between two states. (Correct)**
3.  To change the position of an element.
4.  To apply a filter to an element.

---

### Question 8 (True/False)

**Question:** Animating `width` and `height` is generally better for performance than animating `transform`.
**Time Limit:** 20 seconds
**Answers:**
1.  True
2.  **False (Correct)**

---

### Question 9 (Quiz)

**Question:** Which HTML element is used to create a multi-line text input field in a form?
**Time Limit:** 20 seconds
**Answers:**
1.  `<input type="text">`
2.  **`<textarea>` (Correct)**
3.  `<input type="multiline">`
4.  `<form-input>`

---

### Question 10 (Quiz)

**Question:** To group a set of radio buttons so that only one can be selected at a time, what must they all share?
**Time Limit:** 20 seconds
**Answers:**
1.  The same `id`.
2.  The same `class`.
3.  **The same `name` attribute. (Correct)**
4.  The same `value`.

---

### Question 11 (True/False)

**Question:** The BEM folder structure suggests putting all CSS for a block, its elements, and its modifiers into a single file for that block.
**Time Limit:** 20 seconds
**Answers:**
1.  **True (Correct)**
2.  False

---

### Question 12 (Quiz)

**Question:** In a BEM-structured CSS import, where are all the block files typically imported?
**Time Limit:** 20 seconds
**Answers:**
1.  Directly into the HTML file.
2.  Into a `blocks.css` file.
3.  **Into a page-specific CSS file (e.g., `pages/index.css`). (Correct)**
4.  Into a `vendor.css` file.
