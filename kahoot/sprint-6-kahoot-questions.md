### Sprint 6 Kahoot Quiz

---

### Question 1 (Quiz)

**Question:** What is the main difference between global scope and local scope in JavaScript?
**Time Limit:** 20 seconds
**Answers:**
1.  Global variables are declared with `let`, and local variables with `const`.
2.  **Global variables are accessible from anywhere, while local variables are only accessible within a specific function or block. (Correct)**
3.  Global scope is for functions, and local scope is for objects.
4.  There is no difference.

---

### Question 2 (True/False)

**Question:** Variable shadowing occurs when a variable in an inner scope has the same name as a variable in an outer scope, and the outer variable's value is changed.
**Time Limit:** 20 seconds
**Answers:**
1.  True
2.  **False (Correct)**

---

### Question 3 (Quiz)

**Question:** What is a key advantage of arrow functions (`=>`)?
**Time Limit:** 20 seconds
**Answers:**
1.  They can be used as constructors.
2.  They have their own `arguments` object.
3.  **They provide a more concise syntax for writing function expressions. (Correct)**
4.  They are hoisted to the top of their scope.

---

### Question 4 (Quiz)

**Question:** What is the purpose of rest parameters (`...`) in a function definition?
**Time Limit:** 20 seconds
**Answers:**
1.  To spread the elements of an array into individual arguments.
2.  **To collect an indefinite number of arguments into an array. (Correct)**
3.  To set a default value for a parameter.
4.  To create a shallow copy of an object.

---

### Question 5 (True/False)

**Question:** When an object is passed to a function, a copy of the object is created, so any modifications inside the function do not affect the original object.
**Time Limit:** 20 seconds
**Answers:**
1.  True
2.  **False (Correct)**

---

### Question 6 (Quiz)

**Question:** Which method is used to create a new element in the DOM?
**Time Limit:** 20 seconds
**Answers:**
1.  `document.querySelector()`
2.  `document.appendChild()`
3.  **`document.createElement()` (Correct)**
4.  `document.innerHTML`

---

### Question 7 (Quiz)

**Question:** What is the purpose of the `<template>` element in HTML?
**Time Limit:** 20 seconds
**Answers:**
1.  To define a new CSS style.
2.  To create a new JavaScript function.
3.  **To hold client-side content that is not rendered when a page is loaded but can be instantiated later. (Correct)**
4.  To create a form for user input.

---

### Question 8 (True/False)

**Question:** Event delegation is the practice of attaching an event listener to each individual element in a list.
**Time Limit:** 20 seconds
**Answers:**
1.  True
2.  **False (Correct)**

---

### Question 9 (Quiz)

**Question:** Which property would you use to get the next element sibling of a node?
**Time Limit:** 20 seconds
**Answers:**
1.  `nextSibling`
2.  `childNode`
3.  **`nextElementSibling` (Correct)**
4.  `parentNode`

---

### Question 10 (Quiz)

**Question:** What is the primary difference between `Object.keys()` and a `for...in` loop?
**Time Limit:** 20 seconds
**Answers:**
1.  `Object.keys()` returns an array of values, while `for...in` iterates over keys.
2.  **`Object.keys()` returns only the object's own properties, while `for...in` can include properties from the prototype chain. (Correct)**
3.  `for...in` is faster than `Object.keys()`.
4.  There is no difference.

---

### Question 11 (True/False)

**Question:** Object destructuring allows you to extract properties from an object and assign them to variables with the same name.
**Time Limit:** 20 seconds
**Answers:**
1.  **True (Correct)**
2.  False

---

### Question 12 (Quiz)

**Question:** In array destructuring, how can you skip elements?
**Time Limit:** 20 seconds
**Answers:**
1.  By using the `skip()` method.
2.  By using a negative index.
3.  **By using empty commas (`, ,`). (Correct)**
4.  It's not possible to skip elements.
