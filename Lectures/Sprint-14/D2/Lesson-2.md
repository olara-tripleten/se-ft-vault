
notes:
Practice lesson

This is only visible for instructors
- Create a frontend react application that uses the se-project-express
- Create the register/login using a ModalWithForm alongside the useForm hook
- Explain how to store the token on the localStorage + Common better practices about local storage
- Create the functions to handle the Register + Login requests
- Show/explain that after login in you only get the token, therefore you need to also create a `getUserData` function that does a request to `/users/me`
- Show how to persist the users session while